import React, { useState } from "react";
import { StyleSheet, Text, View, AsyncStorage } from "react-native";

const Help = () => {
  return (
    <View>
      <Text>Help</Text>
    </View>
  );
};

export default Help;
